node_modules/
.venv/
dist/
cli
cli-dev
openclaw/
.env
.env.*
.env
.aider*

# graphify knowledge-graph output (local only)
graphify-out/
.graphify_analysis.json

# Locked harness-evaluation artifacts (manifests, transcripts, diffs, reports).
scripts/eval/results/

# scratch / local notes (never publish)
tmp/

# local agent / tooling state — not for the public repo
.claude/
.antigravitycli/
supabase/
output/

# unrelated private project plans (kept out of the public repo)
docs/superpowers/

# local-only operational instruction copies (full security context — never published)
*.local.md
