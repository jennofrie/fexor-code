# Fexor coding-harness evaluation report

Not run. `acceptance.json` remains unapproved, so Stage B is locked. Use
`bun scripts/eval/runner.ts plan` to inspect the staged evaluation without
making model calls.
